
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: dtnodes.apps.dtwave.com
spec:
  group: apps.dtwave.com
  names:
    kind: DtNode
    listKind: DtNodeList
    plural: dtnodes
    singular: dtnode
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.provider
      name: Provider
      type: string
    - jsonPath: .status.age
      name: Phase
      type: string
    - jsonPath: .status.version
      name: Version
      type: string
    - jsonPath: .status.cpu
      name: Cpu
      type: string
    - jsonPath: .status.memory
      name: Memory
      type: string
    - jsonPath: .status.storage
      name: Storage
      type: string
    - jsonPath: .status.vms
      name: Vms
      type: integer
    - jsonPath: .status.hosts
      name: Hosts
      type: integer
    - jsonPath: .status.networks
      name: Networks
      type: integer
    name: v1
    schema:
      openAPIV3Schema:
        description: DtNode is the Schema for the dtnodes API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: DtNodeSpec defines the desired state of DtNode
            properties:
              ip:
                type: string
              password:
                type: string
              provider:
                description: 'INSERT ADDITIONAL SPEC FIELDS - desired state of cluster
                  Important: Run "make" to regenerate code after modifying this file'
                type: string
              user:
                type: string
            type: object
          status:
            description: DtNodeStatus defines the observed state of DtNode
            properties:
              age:
                type: string
              cpu:
                type: string
              hosts:
                type: integer
              memory:
                type: string
              networks:
                type: integer
              phase:
                description: 'INSERT ADDITIONAL STATUS FIELD - define observed state
                  of cluster Important: Run "make" to regenerate code after modifying
                  this file'
                type: string
              storage:
                type: string
              ttl:
                type: string
              version:
                type: string
              vms:
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
